[Unit]
Description=adminctl Flask API
After=network.target

[Service]
User=www-data
WorkingDirectory=/opt/adminctl
ExecStart=/opt/adminctl/api/venv/bin/gunicorn -w 4 -b 127.0.0.1:5000 "api.app:create_app()"
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
